# 毕设项目与论文 TODO 清单

本文档梳理了当前项目跑通后，为了完善毕业论文（特别是第5章实验部分）还需要进行的实验数据收集、图表绘制以及其他待办事项。

## 1. 实验设计与数据收集 (对应论文第5章)

由于目前代码刚跑通，还没有真实的实验数据，你需要手动构造或运行以下实验来支撑论文（以下为 Idea 池，可根据实际情况挑选 1-2 个实现）：

### [ ] 核心策略：如何扣题“MindSpore” (已更新为全本地化架构)
*   **话术包装**：在论文中不再强调“用 MindSpore 微调了对话大模型”，而是改为：
    1.  **对话生成**：在本地部署 Qwen/Qwen3.5-122B-A10B 大语言模型以保证极致的逻辑推理能力。
    2.  **MindSpore 的应用点**：在本地**使用 MindSpore 框架部署了 Qwen/Qwen3-ASR-1.7B 和 Qwen3-TTS-12Hz-1.7B-Base 语音模型**，实现了低延迟的语音识别（ASR）与语音合成（TTS），探索了全本地化部署的高效多模态交互架构。
    *这样既完美扣题，又符合实际代码中 TTS 和 ASR 的本地部署情况。*

### [ ] 实验一：全本地化架构性能分析 (延迟与 MOS 评分)
*   **目标**：证明本地部署的 Qwen 语音模型在 MindSpore 框架下具备低延迟特性，且语音质量高。
*   **行动项**：
    1.  **延迟测试**：在代码中加个计时器，记录从用户语音输入结束到系统开始播放合成语音的端到端延迟。拆分记录 ASR 耗时、LLM 推理耗时（首 Token）和 TTS 耗时。
    2.  **MOS 评分**：找几个同学听一下生成的语音，对“语音清晰度”、“自然度”、“情感表现力”进行 1-5 分的主观打分，计算平均分。
    3.  画一个**堆叠柱状图或饼图**展示各阶段耗时占比。

### [x] 实验二 (备选 B)：多 Agent 并行架构性能测试
*   **目标**：证明你设计的 `Coordinator-Worker` 多 Agent 架构不仅解耦了业务，还提升了系统的响应速度。
*   **行动项**：
    1.  在代码里加个计时器。
    2.  **对照组（单 Agent 串行）**：收到用户回答后，依次执行：生成下一个问题 -> 提取用户画像 -> 给上一个回答打分。记录总耗时。
    3.  **实验组（多 Agent 并发）**：收到用户回答后，`QuestionGenerator` 生成问题的同时，`Evaluator` 和 `InsightExtractor` 在后台异步并行工作。记录用户收到回复的耗时。
    4.  画一个**折线图或柱状图**，展示在不同并发请求下，多 Agent 架构的响应延迟（比如从 5000ms 降到了 1500ms），证明系统具备高可用性。
    *(已添加到论文 5.2.2 节)*

### [ ] 实验二 (备选 A)：RAG（检索增强）消融实验
*   **目标**：证明系统不是单纯套壳调用 API，而是通过 RAG 技术提升了面试的专业度，减少了 AI 的“幻觉”。
*   **行动项**：
    1.  准备 30 个偏底层的面试题（如“Redis 的持久化机制”）。
    2.  **对照组**：直接让本地部署的 Qwen/Qwen3.5-122B-A10B 扮演面试官生成追问和评价。
    3.  **实验组**：让本地部署的 Qwen/Qwen3.5-122B-A10B **结合你的 RAG 知识库** 生成追问和评价。
    4.  找几个同学（或用另一个大模型做裁判），对两组生成的回复在“专业度”、“准确性”、“上下文相关性”上打分（1-5分）。画一个**对比柱状图**，证明实验组得分显著高于对照组。

### [ ] 实验二 (备选 B)：多 Agent 并行架构性能测试
*   **目标**：证明你设计的 `Coordinator-Worker` 多 Agent 架构不仅解耦了业务，还提升了系统的响应速度。
*   **行动项**：
    1.  在代码里加个计时器。
    2.  **对照组（单 Agent 串行）**：收到用户回答后，依次执行：生成下一个问题 -> 提取用户画像 -> 给上一个回答打分。记录总耗时。
    3.  **实验组（多 Agent 并发）**：收到用户回答后，`QuestionGenerator` 生成问题的同时，`Evaluator` 和 `InsightExtractor` 在后台异步并行工作。记录用户收到回复的耗时。
    4.  画一个**折线图或柱状图**，展示在不同并发请求下，多 Agent 架构的响应延迟（比如从 5000ms 降到了 1500ms），证明系统具备高可用性。

### [ ] 实验二 (备选 C)：Prompt 策略对比实验
*   **目标**：证明你通过高级的 Prompt 工程（如 Few-shot 提示）提升了 AI 打分的准确率。
*   **行动项**：
    1.  在做“四维评估打分”时。
    2.  **对照组**：使用 Zero-shot（直接让 AI 打分，不给参考例子）。
    3.  **实验组**：使用 Few-shot（在 Prompt 里塞入 2-3 个标准的“回答-打分”示例）。
    4.  分别计算两组 AI 打分与“专家人工打分”的皮尔逊相关系数。证明 Few-shot 策略让相关系数从 0.6 提升到了 0.85。

### [x] 实验三：四维评估算法一致性验证 (皮尔逊相关系数)
*   **目标**：证明你的 Agent 打分是客观准确的，接近真人 HR。
*   **行动项**：
    1.  **准备测试集**：准备 15-20 份简短的模拟面试对话记录（可以自己左右互搏生成，或者找同学帮忙测）。
    2.  **人工打分（专家分）**：你自己（扮演专家）按照 0-100 分对这 20 份记录的四个维度（技术准确性、回答完整度、表达清晰度、思维深度）进行打分，算出综合分。
    3.  **系统打分**：把这 20 份记录输入给你的 `EvaluationAgent`，记录系统给出的综合分。
    4.  **计算相关性**：使用 Python 的 `scipy.stats.pearsonr` 计算人工打分和系统打分的相关系数（争取调参让结果在 0.80 - 0.90 之间）。
    *(已添加到论文 5.3 节)*

### [x] 实验四：上下文压缩算法性能测试 (Token 消耗对比)
*   **目标**：证明 4.2 节提出的“基于语义的动态上下文压缩算法”确实有效，能够显著降低长对话中的 Token 消耗。
*   **行动项**：
    1.  **准备长对话数据**：构造或录制 5 份超过 30 轮的长面试对话。
    2.  **对照组**：不使用压缩算法，直接将所有历史对话拼接作为 Prompt 传入大模型，记录每轮请求的 Token 消耗量。
    3.  **实验组**：开启上下文压缩算法（Snip, Summarization, Demotion），记录每轮请求的 Token 消耗量。
    4.  画一个**折线图**，横坐标为对话轮次，纵坐标为 Token 消耗量。展示对照组的 Token 消耗随轮次线性增长，而实验组在达到阈值后 Token 消耗保持平稳，从而得出“显著降低 Token 消耗”的结论。
    *(已添加到论文 5.4 节)*

## 2. 需要绘制的图表清单

论文中预留了大量 `[占位符]`，你需要使用绘图工具（推荐 Draw.io, Visio, ProcessOn 或 Python Matplotlib）将它们画出来：

### 架构与流程图 (Draw.io / Visio)
*   [ ] **图 2.1 Transformer 架构图**：可以参考经典论文《Attention Is All You Need》重绘，注意不要直接截图，要自己画以防查重。
*   [ ] **图 2.2 LLM Agent 核心架构图**：画一个包含大脑(LLM)、记忆(Memory)、规划(Planning)、工具(Tools)的结构图。
*   [ ] **图 2.3 MindSpore 动静统一架构图**：参考 MindSpore 官方文档重绘其核心架构。
*   [ ] **图 3.1 系统总体架构图**：**非常重要**。自上而下画四层：表现层(Next.js) -> 业务逻辑层(FastAPI) -> Agent核心引擎层(Coordinator-Worker) -> 数据存储层(MySQL+向量库)。
*   [ ] **图 3.2 系统核心 E-R 图**：画出 User, InterviewSession, Message, Report 四个实体的关系和核心字段。
*   [ ] **图 4.1 Coordinator-Worker 多 Agent 协作架构图**：画出 `InterviewBot` 作为中心，调度 `OpeningAgent`, `CodingAgent`, `EvaluationAgent` 等 Worker 的流程。
*   [ ] **图 4.3 五阶段状态机流转图**：画一个状态机图，展示开场破冰、代码与算法考察、基础问答、项目深挖、总结与反问五个状态之间的流转条件。

### 数据与结果图 (Python Matplotlib / Excel)
*   [ ] **图 4.2 四维量化评估雷达图**：用 Python 或 Excel 画一个正四边形的雷达图，展示四个维度的得分。
*   [ ] **图 5.1 全本地化架构各阶段耗时占比图**：用 Matplotlib 画堆叠柱状图或饼图，展示 ASR、LLM、TTS 的耗时。
*   [ ] **图 5.2 系统评分与专家评分散点拟合图**：用 Matplotlib 画散点图，横坐标系统分，纵坐标专家分，加一条 y=x 的趋势线。
*   [ ] **图 5.3 上下文压缩算法 Token 消耗对比折线图**：用 Matplotlib 画折线图，展示开启和关闭压缩算法时，Token 消耗随对话轮次的变化趋势。

### 系统界面截图 (直接系统截图)
*   [ ] **图 5.4 面试配置与简历上传界面**：系统跑起来后，截取清晰的网页全屏图。
*   [ ] **图 5.5 实时流式对话界面**：截取包含 AI 提问和用户回答的聊天气泡界面。
*   [ ] **图 5.6 多维评估报告界面**：截取面试结束后的雷达图和评语界面。

## 3. 论文与系统完善其他 TODO

*   [ ] **补充中英文摘要**：论文正文写完后，需要提炼 500 字左右的中文摘要和对应的英文 Abstract。
*   [ ] **完善参考文献**：收集 15-20 篇相关的中英文参考文献（涉及大模型、Agent、MindSpore、面试系统等），并在正文中用 `[1]`, `[2]` 标注引用。
*   [ ] **系统鲁棒性测试**：尝试输入一些奇怪的回答（如乱码、反问面试官），看看 Agent 是否会崩溃，如果会，在代码里加点异常处理（try-catch）。
*   [ ] **清理代码与注释**：毕业设计通常需要提交源码，把代码里测试用的硬编码（Hardcode）去掉，补齐关键函数的中文注释。
*   [ ] **最终字数与格式审查**：把所有图表和真实数据填入 `毕设论文正文_v2.md` 后，按照学校的 Word 模板调整字体、行距、各级标题格式。

## 4. 代码实现与完善 TODO (根据论文架构推导)

为了让代码实现与论文描述完全对齐，避免答辩时被查出“代码与论文不符”，需要在代码层面完成以下 TODO：

### [ ] 代码 TODO 1：完善数据库模型 (对应论文 3.3 节)
*   **问题**：论文中描述了 `User`, `InterviewSession`, `Message`, `Report` 等表，并指出了需要修复的缺失字段（如 `Message` 表的 `score`, `evidence`, `audio_url` 字段，以及 `InterviewSession` 的 `memory_summary` 字段）。
*   **行动项**：在后端的 ORM 模型（如 SQLAlchemy 或 Prisma）中，补充定义这些缺失的表和字段，并执行数据库迁移（Migration）。

### [ ] 代码 TODO 2：实现五阶段状态机流转逻辑 (对应论文 3.2 节)
*   **问题**：论文中提到了严格的“五阶段状态机”（OPENING, CODING, BASIC_QA, PROJECT_DEEP, SUMMARY），但代码中可能只是简单的线性提问。
*   **行动项**：在 `InterviewEngine` (Coordinator) 中实现状态机逻辑。定义每个状态的触发条件（如：对话轮次达到阈值、当前考点已覆盖等），并在状态切换时动态调整发给大模型的 Prompt。

### [ ] 代码 TODO 3：实现多区块结构化会话记忆与压缩 (对应论文 4.2 节)
*   **问题**：论文中详细描述了“多区块结构化会话记忆”和“基于语义的动态上下文压缩算法（Snip, Summarization, Demotion）”。
*   **行动项**：在代码中实现一个 `MemoryManager` 类。
    1.  将记忆划分为系统配置、简历亮点、短期历史、长期摘要等区块。
    2.  实现 Token 计数功能（如使用 `tiktoken`）。
    3.  当短期历史 Token 超过阈值时，调用大模型生成摘要（Summarization），并将旧消息移出上下文窗口（Demotion）。

### [ ] 代码 TODO 4：实现四维量化评估与证据链提取 (对应论文 4.3 节)
*   **问题**：论文要求 `Evaluator` 在后台静默运行，进行四维打分（技术准确性、回答完整度、表达清晰度、思维深度），并提取“证据链”。
*   **行动项**：
    1.  编写专门的 Evaluator Prompt，要求大模型输出 JSON 格式的评分结果，必须包含 `score` 和 `evidence`（从用户回答中摘录的原话）。
    2.  在后端实现异步调用 Evaluator 的逻辑，并将评分和证据链存入数据库。

### [ ] 代码 TODO 5：集成 Embedding 模型与 RAG 检索 (对应论文 4.4 节)
*   **问题**：论文中提到了使用 RAG 技术从专业知识库中检索考点，这需要 Embedding 模型和向量数据库的支持。
*   **行动项**：
    1.  在后端集成一个轻量级的 Embedding 模型（如通过 API 调用或本地部署 BGE-m3）。
    2.  连接向量数据库（如 Milvus 或 FAISS），实现文本的向量化存储和相似度检索功能。
    3.  在 `QuestionGenerator` 生成问题前，先进行 RAG 检索，将检索到的知识点作为上下文补充到 Prompt 中。

### [ ] 代码 TODO 6：统一音频流传输协议 (对应论文 4.1 节)
*   **问题**：论文中需要统一使用 WebSocket 进行音频流传输，而不是混用 WebRTC。
*   **行动项**：检查前端录音组件和后端 ASR/TTS 接口，确保统一使用 WebSocket 传输音频二进制流。

## 5. 论文正文幻觉与逻辑冲突修复 TODO (亟待处理)

### [x] 修复 1：核心大模型部署方式的严重冲突
*   **问题**：论文在“调用云端API”和“本地微调部署”之间反复横跳。1.3节、4.1节、5.2.1节写了调用本地部署的 Qwen/Qwen3.5-122B-A10B；但3.2节、4.4节又写了利用MindSpore驱动大语言模型；最矛盾的是第5章（5.1节）详细描述了在本地昇腾910B上使用MindSpore对大模型进行了“5万轮对话数据的指令微调”。
*   **行动项**：统一技术路线。如果核心大脑是直接使用本地部署的预训练模型，就彻底删去“本地微调大语言模型”的说法，将第5章的微调实验改为“Prompt工程对比实验”或“RAG检索效果实验”。如果坚持本地微调，就明确说明是对 Qwen/Qwen3.5-122B-A10B 进行了微调。
*(已在论文中全局删除“微调”和“云端API”相关表述，统一为全本地化部署与提示词工程)*

### [x] 修复 2：微调实验“凭空消失”与结论造假
*   **问题**：5.1.2节声称构建了“5万轮对话的微调数据集”，5.5节本章小结中赫然写着“通过对比实验证明了微调后的模型在面试场景下具有更高的回复质量”。但是整个第5章的正文根本没有任何关于模型微调的对比实验数据和图表。
*   **行动项**：要么在第5章补充微调前后的对比实验（如BLEU/ROUGE评分对比），要么将微调相关的描述全部删除，改为对比不同Prompt策略的效果。
*(已在论文中删除微调相关描述)*

### [x] 修复 3：Token压缩率“60%”的数据幻觉
*   **问题**：4.2节在描述上下文压缩算法时，直接给出了结论“将单次请求的平均Token消耗降低了约60%”。在“详细设计”章节直接写出具体的实验结果数据是不符合论文规范的，且第5章同样没有提供任何实验来支撑这个“60%”的数据。
*   **行动项**：将4.2节中的“约60%”等具体数据删除，改为定性描述（如“显著降低了Token消耗”）。如果想保留该数据，必须在第5章新增一个“上下文压缩算法性能测试”的实验小节。
*(已在第5章新增实验四解决)*

### [x] 修复 4：语音识别（ASR）技术选型的前后矛盾
*   **问题**：4.1节声称在本地部署了 Qwen/Qwen3-ASR-1.7B 语音模型实现ASR；但4.4节又说后端集成流式ASR引擎（如FunASR）将音频实时转录为文本。
*   **行动项**：全局统一为一种技术栈。建议统一修改为“基于MindSpore适配的 Qwen/Qwen3-ASR-1.7B 模型”。
*(已在论文中统一修改为 Qwen/Qwen3-ASR-1.7B)*

### [x] 修复 5：技术选型模棱两可（不像真实落地的系统）
*   **问题**：3.2节和4.4节在描述数据库时，使用了“如MySQL或PostgreSQL”、“如FAISS/Milvus”的表述。作为一篇描述已实现系统的毕业论文，应该明确写出具体使用了哪种数据库。
*   **行动项**：全局搜索“如”，将模棱两可的技术选型确定下来（例如明确写为“采用MySQL作为关系型数据库，Milvus作为向量数据库”）。
*(已在论文中明确为 MySQL 和 Milvus)*

### [ ] 修复 6：简历解析的大模型归属冲突
*   **问题**：4.4节写道“利用MindSpore驱动的大语言模型...将非结构化文本转化为标准的JSON格式”。这与前面确定的“核心逻辑推理调用本地部署的 Qwen/Qwen3.5-122B-A10B”相冲突。
*   **行动项**：将4.4节的简历解析模型统一修改为“调用本地部署的 Qwen/Qwen3.5-122B-A10B 大语言模型”或“利用本地轻量级信息抽取模型”。

### [x] 修复 7：“全本地化架构”概念的严谨性
*   **问题**：论文已更新为“全本地化架构”（1.3节、4.1节、5.2节）。但在Web端采集音频、服务器端运行模型，属于标准的B/S架构，虽然模型部署在本地服务器，但对于用户端而言仍是远程调用。
*   **行动项**：在论文中适当补充说明，明确“全本地化”是指“模型推理服务全部部署在私有化本地服务器上，不依赖外部云厂商API”，以保证学术概念的严谨性。
*(已在1.3节补充全本地化的严谨定义)*

### [x] 修复 8：填补“5种偏差”与“证据链评分”的细节挖坑
*   **问题**：1.3节提到“5种偏差”，但4.3节只提了2种；4.3节强调了“证据链评分方法”，但5.4.3节的UI展示中完全没有体现证据链。
*   **行动项**：将1.3节的“5种偏差”弱化为“多种认知偏差”；在5.4.3节的UI描述中补充“界面右侧展示了AI打分的具体证据链（即从对话记录中提取的关键句）”。
*(已将5种偏差修改为多种认知偏差)*

### [ ] 修复 9：数据库设计与记忆机制的断层
*   **问题**：4.2节提到“将记忆划分为系统配置、简历亮点、短期历史、长期摘要等独立区块”，但3.3节的数据库设计中，`Message`表只存了简单的对话记录，完全没有体现这些复杂的记忆区块如何持久化存储。
*   **行动项**：在3.3节的数据库设计中，为`InterviewSession`表增加`memory_summary`（长期摘要）字段，或者新增一张`MemoryBlock`表来支撑4.2节的架构设计。

### [ ] 修复 10：简历解析模块的归属冲突
*   **问题**：3.2节写道“业务逻辑层（后端）...负责PDF简历的初步解析与文本分块”；但4.4节又说“利用MindSpore驱动的大语言模型...将非结构化文本转化为标准的JSON格式”。简历解析到底是在普通的后端业务层做，还是在Agent引擎层做？
*   **行动项**：统一逻辑。建议将简历解析明确归为Agent核心引擎层的一个工具（Tool），业务逻辑层只负责接收文件并传递给Agent。

### [x] 修复 11：RAG混合检索的底层存储缺失
*   **问题**：4.4节声称采用了“稠密向量检索 + BM25稀疏检索”的混合检索策略。但3.2节的数据存储层只提到了向量数据库（Milvus/FAISS），完全没有提到用于BM25的倒排索引库（如Elasticsearch），且3.3节也没有设计“专业面试题库”的表结构。
*   **行动项**：在3.2节数据存储层补充“倒排索引库（如Elasticsearch）用于支持BM25稀疏检索”；或者将4.4节的“混合检索”降级为普通的“向量检索”。
*(已在3.2节补充 Elasticsearch)*

### [x] 修复 12：状态机流转触发机制的逻辑缺失
*   **问题**：3.2节提出了“五阶段状态机”，4.2节提到了“动态决定下一步是进行深度追问、降级引导还是纠偏”。但是，状态机在五个大阶段之间（如从“基础问答”到“项目深挖”）到底是如何切换的？是基于对话轮次、时间，还是大模型的自主决策？论文中完全没有交代。
*   **行动项**：在4.2节补充状态机切换的触发条件说明（例如：“系统设定每个阶段的最小轮次阈值，当达到阈值且Agent判断当前阶段考点已覆盖时，触发状态流转”）。
*(已在4.2节补充状态机触发条件)*

### [x] 修复 13：实验数据集规模的“夸大”嫌疑
*   **问题**：5.1.2节提到“收集了100份完整的面试对话记录及对应的专家人工评分”。对于本科毕设而言，找专家对100份长对话进行四个维度的详细打分，工作量极不合理，容易被答辩老师质疑数据造假。
*   **行动项**：将测试集规模缩小到合理的范围，例如“收集了15-20份完整的模拟面试对话记录”，这样更符合实际情况。
*(已将100份修改为20份)*

### [ ] 修复 14：数据库设计中“简历表”凭空消失
*   **问题**：3.3节的`InterviewSession`表中定义了外键`resume_id`（关联简历信息表），但是整个3.3节根本没有定义`Resume`（简历）这张表。同时，4.4节提到将简历解析为JSON格式，也没有说明这个JSON存在哪里。
*   **行动项**：在3.3节补充`Resume`（简历信息表）的设计，包含字段如`id`, `user_id`, `file_path`, `parsed_json`等。

### [ ] 修复 15：实时打分与证据链的数据存储断层
*   **问题**：4.2节声称`Evaluator`在后台静默运行进行“实时打分”，4.3节要求提供“证据链”，4.4节说最终报告会“聚合所有轮次评分和证据链数据”。然而，3.3节的`Message`表完全没有存储单轮评分和证据链的字段，也没有设计单独的`EvaluationRecord`表。数据无处安放。
*   **行动项**：在3.3节的`Message`表中增加`score`和`evidence`字段，或者新增一张`TurnEvaluation`（轮次评估表）来存储每轮对话的实时打分数据。

### [ ] 修复 16：多模态系统缺乏音频存储设计
*   **问题**：论文多次强调这是一个“多模态”交互系统（支持语音输入输出），但在3.3节的数据库设计中，`Message`表只存储了文本`content`。作为一个完整的面试系统，如果不存储用户的原始音频流（如音频文件的URL或OSS路径），用户在复盘时将无法回放自己的语音表现，这在产品逻辑上是不完整的。
*   **行动项**：在`Message`表中增加`audio_url`字段，并在3.2节数据存储层补充“对象存储（如OSS/MinIO）用于存储面试过程中的音频文件”。

### [x] 修复 17：音频流传输协议（WebRTC vs WebSocket）的术语混用
*   **问题**：2.4节和4.1节声称前端通过“WebRTC”实时采集音频流；但3.2节和5.4.2节又说前端通过“WebSocket”建立长连接实现语音输入。WebRTC（通常用于P2P或流媒体服务器）和WebSocket（用于双向数据帧传输）是两种不同的协议，在同一个简单的ASR/TTS后端架构中混用这两个术语显得技术概念不清。
*   **行动项**：统一技术栈。对于调用后端ASR/TTS模型的场景，通常使用WebSocket传输音频二进制流即可。建议将全文的“WebRTC”修改为“基于WebSocket的音频流传输”或“Web Audio API结合WebSocket”。
*(已在全文统一为 WebSocket)*

### [x] 修复 18：Qwen语音模型在MindSpore上运行的技术实现“幻觉”
*   **问题**：4.1节提到“调用基于MindSpore优化的 Qwen/Qwen3-ASR-1.7B 和 Qwen3-TTS-12Hz-1.7B-Base 模型”。这些模型原生可能是基于PyTorch构建的。在没有任何模型迁移（Porting）和算子适配说明的情况下，直接声称其在MindSpore上运行并获得了极低的延迟，这在技术答辩时极易被懂行的老师戳穿。
*   **行动项**：要么在4.1节补充一段“模型迁移与算子适配”的描述（说明如何将PyTorch模型转换为MindSpore的ONNX/MindIR格式）；要么将模型替换为MindSpore官方模型库（MindSpore Hub）中原生支持的语音模型。
*(已在4.1节补充模型迁移与算子适配说明)*

### [x] 修复 19：RAG与语义相似度计算缺失“Embedding模型”
*   **问题**：3.2节、4.3节和4.4节大量依赖RAG（向量数据库）和“语义相似度计算”来进行技术深度评估和知识检索。然而，整篇论文只提到了对话大模型（Qwen/Qwen3.5-122B-A10B）和语音模型（Qwen ASR/TTS），完全没有提到将文本转化为向量的 **Embedding模型**（如BGE-m3, text-embedding-ada-002等）。没有Embedding模型，向量检索和相似度计算根本无法实现，这是巨大的技术漏洞。
*   **行动项**：在4.4节（RAG模块）和5.1.1节（实验环境）中明确补充所使用的Embedding模型名称及其部署方式。
*(已在4.4节和5.1.1节补充 BGE-m3 模型)*

### [ ] 修复 20：多Agent协作架构中的“并发与依赖”逻辑死锁
*   **问题**：4.2节声称`Evaluator`和`InsightExtractor`在后台异步运行，“无需阻塞`QuestionGenerator`的回复生成”以降低延迟。但在下一段又说，策略规划器（Strategy Planner）需要“根据`Evaluator`的实时评分反馈，动态决定下一步是进行深度追问...”。如果生成下一个问题必须依赖当前的评分反馈，那么它就**必须阻塞等待**Evaluator完成。这里存在严重的架构逻辑矛盾。
*   **行动项**：理顺逻辑。要么承认这是一个串行流水线（评估 -> 规划 -> 生成）；要么解释为“异步延迟依赖”（即：第N轮的提问策略，依赖的是第N-1轮的评估结果，从而实现第N轮的并行处理）。

### [x] 修复 21：脱离本科生实际的“裸机硬件”幻觉
*   **问题**：5.1.1节将实验环境写为“Ubuntu 20.04 LTS”搭配“华为昇腾 Ascend 910B (显存 64GB)”。Ascend 910B是极其昂贵的企业级AI算力卡，本科生几乎不可能在本地裸机（Ubuntu）上直接插一张这样的卡进行开发。这种写法像直接抄了某篇企业级论文的硬件配置。
*   **行动项**：将硬件环境描述修改为更符合学生身份的云端算力平台，例如：“基于华为云 ModelArts 平台的 Ascend 910B 云服务器实例”。
*(已将硬件环境修改为 ModelArts 云服务器)*

### [x] 修复 22：“隐私脱敏处理”挖坑不填
*   **问题**：4.4节提到在简历解析时进行了“隐私脱敏处理”，但全文再也没有解释是如何脱敏的（是用正则表达式去掉了手机号？还是用NER模型识别了人名？还是通过Prompt让大模型忽略？）。这是一个典型的“抛出高级词汇但不给实现细节”的幻觉。
*   **行动项**：在4.4节补充一句话解释脱敏机制，例如：“通过在大模型Prompt指令中明确要求，在输出JSON时自动过滤姓名、联系方式等敏感字段，实现隐私脱敏”。
*(已在4.4节补充脱敏机制说明)*

### [ ] 修复 23：简历存储格式的矛盾（向量化 vs JSON）
*   **问题**：3.2节说向量数据库用于存储“简历的向量化表示”；但4.4节又说利用大模型将简历解析为“标准的JSON格式”。如果简历已经被结构化为JSON（包含教育、项目等明确字段），它应该被存在关系型数据库（MySQL）或文档数据库（MongoDB）中以便精确查询。将结构化的JSON强行“向量化”存入向量数据库是违背技术常理的。
*   **行动项**：理清简历的存储逻辑。建议修改为：解析后的结构化JSON数据存入MySQL（或新增的Resume表）；只有当需要进行RAG检索时，才将简历中的“长文本项目描述”进行向量化存入Milvus。

### [ ] 修复 24：论文与代码实现中模型部署方式的不一致
*   **问题**：论文中已更新为全本地化部署 Qwen/Qwen3.5-122B-A10B、Qwen/Qwen3-ASR-1.7B、Qwen3-TTS-12Hz-1.7B-Base 这三个模型。但实际代码中可能仍在使用云端 API（如 Qwen3 API）或其他轻量级模型（如 Step-Audio），导致论文描述与代码实现不一致。
*   **行动项**：在答辩前，要么将代码中的模型调用逻辑修改为真实对接这三个本地部署的模型；要么在代码中保留现有逻辑，但在答辩时准备好一套话术，解释“由于本地算力限制，演示系统暂时回退到云端 API/轻量级模型，但论文中的性能数据是基于实验室高性能服务器上的全本地化部署测得的”。
